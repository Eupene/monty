#include "monty.h"
/**
 * f_stack - it prints the top
 * @head: the stack head
 * @counter: a line_number
 * Return: null
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
